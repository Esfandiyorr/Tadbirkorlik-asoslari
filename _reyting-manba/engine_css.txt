/* ================== REYTING TIZIMI ================== */
.rt-chip{display:inline-flex;align-items:center;gap:7px;border:1px solid var(--line);background:var(--panel);
  border-radius:999px;padding:5px 12px;font-size:13px;font-weight:700;cursor:pointer;transition:.18s;color:var(--ink)}
.rt-chip:hover{transform:translateY(-2px);box-shadow:0 10px 22px -12px rgba(0,0,0,.45)}
.rt-chip b{color:var(--green)}
.rt-nav{display:inline-flex;align-items:center;gap:4px;border:1px solid var(--line);background:var(--panel);
  color:var(--ink);border-radius:11px;padding:6px 9px;font-size:12px;font-weight:700;cursor:pointer;
  text-decoration:none;transition:.16s;white-space:nowrap}
.rt-nav:hover{transform:translateY(-2px);border-color:var(--blue)}
.rt-nav.cur{background:linear-gradient(135deg,var(--green),var(--teal));color:#fff;border-color:transparent;cursor:default}
.rt-nav.cur:hover{transform:none}
.rt-nav.lock{opacity:.5;cursor:not-allowed;border-style:dashed}
.rt-nav.lock:hover{transform:none;border-color:var(--line)}
@media(max-width:860px){
  .topbar{flex-wrap:wrap;row-gap:8px}
  .topbar .spacer{display:none}
  .topbar .tools{flex:1 1 100%;min-width:0;justify-content:flex-start;order:6;gap:6px}
  .topbar .tools .btn,.topbar .rt-chip{padding:6px 9px;font-size:12px}
  #btnTimer,#btnHelp{display:none}
  #rtNav{order:5;flex:1 1 100%;min-width:0;margin-left:0;justify-content:flex-start}
  .rt-nav{font-size:11.5px;padding:5px 8px}
}
.rt-ov{position:fixed;inset:0;z-index:140;display:none;padding:20px;overflow:auto;
  background:color-mix(in srgb,var(--bg) 90%,transparent);backdrop-filter:blur(12px)}
.rt-ov.show{display:block;animation:fade .25s ease}
.rt-box{max-width:1000px;margin:0 auto;background:var(--panel);border:1px solid var(--line);border-radius:22px;
  padding:22px;box-shadow:var(--shadow)}
.rt-head{display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin-bottom:14px}
.rt-head h2{margin:0;flex:1;font-size:clamp(19px,2.5vw,28px)}
.rt-tabs{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px}
.rt-tab{border:1px solid var(--line);background:var(--panel2);color:var(--ink);border-radius:12px;padding:8px 14px;
  font-size:13.5px;font-weight:700;cursor:pointer;font-family:inherit;transition:.15s}
.rt-tab:hover{border-color:var(--blue)}
.rt-tab.on{background:linear-gradient(135deg,var(--violet),var(--blue));color:#fff;border-color:transparent}
.rt-pane{display:none}.rt-pane.on{display:block}
.rt-table{width:100%;border-collapse:separate;border-spacing:0;font-size:14.5px}
.rt-table th{background:linear-gradient(135deg,var(--violet),var(--blue));color:#fff;padding:10px 12px;text-align:left;font-weight:700}
.rt-table th:first-child{border-radius:12px 0 0 0}.rt-table th:last-child{border-radius:0 12px 0 0}
.rt-table td{padding:10px 12px;border-bottom:1px solid var(--line)}
.rt-table tr.me td{background:color-mix(in srgb,var(--green) 14%,transparent);font-weight:700}
.rt-medal{font-size:17px}
.rt-form{display:grid;gap:12px;max-width:420px;margin:0 auto}
.rt-form label{font-size:13.5px;font-weight:700;color:var(--muted)}
.rt-form input,.rt-form select{width:100%;margin-top:5px;padding:11px 13px;border:1.5px solid var(--line);border-radius:12px;
  background:var(--panel2);color:var(--ink);font-size:15px;font-weight:600;font-family:inherit;outline:none}
.rt-form input:focus,.rt-form select:focus{border-color:var(--blue)}
.rt-gate{position:fixed;inset:0;z-index:120;display:grid;place-items:center;padding:20px;overflow:auto;
  background:linear-gradient(160deg,var(--bg),var(--bg2))}
.rt-gate .rt-box{max-width:560px;width:100%}
.rt-bar{height:9px;border-radius:99px;background:var(--chip);overflow:hidden;margin-top:6px}
.rt-bar i{display:block;height:100%;border-radius:99px;background:linear-gradient(90deg,var(--green),var(--cyan))}
.rt-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:12px}
.rt-card{background:var(--panel2);border:1px solid var(--line);border-radius:16px;padding:14px}
.rt-card span{display:block;font-size:12px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.4px}
.rt-card b{display:block;font-size:24px;color:var(--green);margin-top:3px}
.rt-topic{display:flex;align-items:center;gap:12px;background:var(--panel2);border:1px solid var(--line);
  border-radius:14px;padding:12px 14px;margin-bottom:9px}
.rt-topic .t{flex:1}.rt-topic .t b{display:block;font-size:15px}
.rt-topic .t span{font-size:12.5px;color:var(--muted)}
.rt-topic.locked{opacity:.72}
.rt-toast{position:fixed;right:18px;bottom:74px;z-index:130;background:linear-gradient(135deg,var(--green),var(--teal));
  color:#fff;font-weight:800;padding:11px 18px;border-radius:14px;box-shadow:var(--shadow);opacity:0;transform:translateY(14px);
  transition:.35s;pointer-events:none;font-size:14.5px}
.rt-toast.show{opacity:1;transform:none}
.rt-note{font-size:12.5px;color:var(--muted);margin-top:10px;line-height:1.5}
@media print{.rt-ov,.rt-gate,.rt-toast{display:none!important}}
.rt-badge{display:inline-block;min-width:20px;padding:1px 7px;border-radius:99px;font-size:12.5px;font-weight:800;
  background:linear-gradient(135deg,var(--violet),var(--blue));color:#fff;vertical-align:middle}
.rt-table td .btn{padding:6px 10px;font-size:12.5px}
.rt-tclink{display:block;width:100%;margin-top:10px;border:1px dashed var(--line);background:none;color:var(--muted);
  border-radius:12px;padding:9px;font-size:12.5px;font-weight:700;cursor:pointer;font-family:inherit;transition:.16s}
.rt-tclink:hover{border-color:var(--violet);color:var(--ink)}
.rt-diag{margin-top:10px;background:var(--panel2);border:1px solid var(--line);border-radius:14px;padding:12px 14px;
  font-size:13.5px;line-height:1.85}
.rt-diag i{font-style:normal;font-weight:800;font-family:ui-monospace,Menlo,Consolas,monospace}
.rt-diag .rt-note{margin-top:8px}
/* --- pochta, guruhlar va jurnal --- */
.rt-sec{background:var(--panel2);border:1px solid var(--line);border-radius:16px;padding:14px 16px;margin-top:14px}
.rt-sec h4{margin:0 0 8px;font-size:14.5px}
.rt-mail textarea{width:100%;min-height:120px;border:1px solid var(--line);border-radius:12px;padding:10px 12px;
  font-family:inherit;font-size:14px;background:var(--panel);color:var(--ink);resize:vertical}
.rt-subtabs{display:flex;gap:7px;margin:0 0 12px;flex-wrap:wrap}
.rt-sub{border:1.5px solid var(--line);background:var(--panel2);color:var(--ink);border-radius:12px;
  padding:9px 15px;font-size:13.5px;font-weight:800;cursor:pointer;font-family:inherit;transition:.16s}
.rt-sub:hover{border-color:var(--violet)}
.rt-sub.on{background:linear-gradient(135deg,var(--violet),var(--blue));color:#fff;border-color:transparent}
.rt-gsec{margin-top:14px}
.rt-gsec h4{margin:0 0 7px;font-size:14px;color:var(--muted)}
.rt-gchips{display:flex;gap:7px;flex-wrap:wrap;margin-bottom:12px}
.rt-gchip{border:1.5px solid var(--line);background:var(--panel2);color:var(--ink);border-radius:999px;
  padding:7px 14px;font-size:13px;font-weight:800;cursor:pointer;font-family:inherit;transition:.16s}
.rt-gchip:hover{border-color:var(--green)}
.rt-gchip.on{background:linear-gradient(135deg,var(--green),var(--teal));color:#fff;border-color:transparent}
.rt-gchip.on .rt-badge{background:rgba(255,255,255,.28)}
/* --- Guruh kvadratchalari: bosilganda o'sha guruh ochiladi --- */
.rt-gtiles{display:grid;grid-template-columns:repeat(auto-fill,minmax(112px,1fr));gap:10px;margin-bottom:10px}
.rt-gtile{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:5px;
  aspect-ratio:1;border:1.5px solid var(--line);background:var(--panel2);color:var(--ink);
  border-radius:16px;cursor:pointer;font-family:inherit;padding:10px;transition:.16s}
.rt-gtile:hover{border-color:var(--violet);transform:translateY(-2px);box-shadow:0 10px 20px -14px rgba(0,0,0,.4)}
.rt-gtile .ic{font-size:22px;line-height:1}
.rt-gtile b{font-size:12.5px;text-align:center;word-break:break-word;line-height:1.25}
.rt-gtile.on{background:linear-gradient(135deg,var(--violet),var(--blue));color:#fff;border-color:transparent}
.rt-gtile.on .rt-badge{background:rgba(255,255,255,.28)}
/* --- Talaba qidiruvi --- */
.rt-gsearch{margin-bottom:12px}
.rt-gsearch input{width:100%;padding:11px 13px;border:1.5px solid var(--line);border-radius:12px;
  background:var(--panel2);color:var(--ink);font-size:14.5px;font-weight:600;font-family:inherit;outline:none}
.rt-gsearch input:focus{border-color:var(--blue)}
#tcSearchRes{display:flex;flex-direction:column;gap:6px;margin-top:8px}
.rt-gsearch-item{display:flex;align-items:center;justify-content:space-between;gap:10px;width:100%;
  text-align:left;border:1px solid var(--line);background:var(--panel);color:var(--ink);
  border-radius:12px;padding:10px 13px;font-family:inherit;font-size:13.5px;cursor:pointer;transition:.14s}
.rt-gsearch-item:hover{border-color:var(--violet)}
.rt-gsearch-item span{color:var(--muted);font-weight:700;white-space:nowrap}
/* --- Noto'g'ri yozilgan guruhni bir bosishda tuzatish --- */
.rt-strayban{display:flex;flex-direction:column;gap:8px;margin-bottom:12px}
.rt-stray-item{display:flex;align-items:center;justify-content:space-between;gap:10px;flex-wrap:wrap;
  border:1.5px solid #f59e0b;background:color-mix(in srgb,#f59e0b 12%,transparent);
  border-radius:14px;padding:10px 14px;font-size:13px}
.rt-stray-item b{font-weight:800}
.rt-daynav{display:flex;gap:7px;align-items:center;flex-wrap:wrap;margin-bottom:10px}
.rt-daynav input[type="date"]{border:1px solid var(--line);background:var(--panel);color:var(--ink);
  border-radius:11px;padding:8px 10px;font-family:inherit;font-size:13.5px;font-weight:700}
.rt-jtab td{vertical-align:middle}
.rt-jtab tr.nbrow{opacity:.6}
.rt-marks{white-space:nowrap}
.rt-mb{width:34px;height:34px;margin-right:3px;border:1.5px solid var(--line);background:var(--panel);
  color:var(--ink);border-radius:10px;font-size:14px;font-weight:800;cursor:pointer;font-family:inherit;transition:.14s}
.rt-mb:hover{transform:translateY(-2px);border-color:var(--violet)}
.rt-mb.on{color:#fff;border-color:transparent;transform:none}
.rt-mb.b1,.rt-mk.b1{background:#e11d48}
.rt-mb.b2,.rt-mk.b2{background:#f97316}
.rt-mb.b3,.rt-mk.b3{background:#f59e0b}
.rt-mb.b4,.rt-mk.b4{background:#0891b2}
.rt-mb.b5,.rt-mk.b5{background:#12a150}
.rt-nb{border:1.5px solid var(--line);background:var(--panel);color:var(--ink);border-radius:10px;
  padding:7px 11px;font-size:12.5px;font-weight:800;cursor:pointer;font-family:inherit;white-space:nowrap;transition:.14s}
.rt-nb:hover{border-color:var(--red)}
.rt-nb.on{background:var(--red);color:#fff;border-color:transparent}
.rt-note-in{width:100%;min-width:120px;border:1px solid var(--line);background:var(--panel);color:var(--ink);
  border-radius:10px;padding:7px 10px;font-family:inherit;font-size:13px}
.rt-mk{display:inline-block;min-width:30px;text-align:center;border-radius:9px;padding:3px 9px;
  font-weight:800;color:#fff;font-size:13.5px}
.rt-mk.nb{background:var(--muted)}
.rt-authbar{display:flex;align-items:center;gap:12px;border:1.5px dashed var(--line);background:var(--panel2);
  border-radius:14px;padding:10px 14px;margin-bottom:12px}
.rt-authbar.on{border-style:solid;border-color:var(--green);
  background:color-mix(in srgb,var(--green) 8%,transparent)}
.rt-authbar .ic{font-size:22px}
.rt-authbar .t{flex:1;min-width:0}
.rt-authbar .t b{display:block;font-size:14px}
.rt-authbar .t span{display:block;font-size:12px;color:var(--muted);line-height:1.45}
.rt-steps{margin:10px 0 0;padding-left:20px;font-size:13.5px;line-height:1.75;color:var(--ink2)}
.rt-steps li{margin-bottom:5px}
/* --- dars bahosi --- */
.rt-gtabs{display:flex;gap:7px;margin-bottom:14px}
.rt-gt{flex:1;border:1.5px solid var(--line);background:var(--panel2);color:var(--ink);border-radius:12px;
  padding:10px;font-size:13.5px;font-weight:800;cursor:pointer;font-family:inherit;transition:.16s}
.rt-gt:hover{border-color:var(--violet)}
.rt-gt.on{background:linear-gradient(135deg,var(--green),var(--teal));color:#fff;border-color:transparent}
.rt-mark{display:flex;align-items:center;gap:14px;border-radius:18px;padding:14px 18px;color:#fff;
  background:linear-gradient(135deg,var(--muted),#64748b)}
.rt-mark.b1{background:linear-gradient(135deg,#e11d48,#be123c)}
.rt-mark.b2{background:linear-gradient(135deg,#f97316,#ea580c)}
.rt-mark.b3{background:linear-gradient(135deg,#f59e0b,#d97706)}
.rt-mark.b4{background:linear-gradient(135deg,#0891b2,#0e7490)}
.rt-mark.b5{background:linear-gradient(135deg,#12a150,#0b7a3c)}
.rt-mark .n{font-size:44px;font-weight:900;line-height:1;min-width:52px;text-align:center}
.rt-mark .t b{display:block;font-size:16px}
.rt-mark .t span{display:block;font-size:13px;opacity:.9;margin-top:2px}
.rt-part{margin-bottom:9px}
.rt-part .row2{display:flex;justify-content:space-between;font-size:13px;margin-bottom:3px}
.rt-part .row2 i{font-style:normal;color:var(--muted)}
.rt-scale{font-size:13px;color:var(--muted);line-height:2}
.rt-mk{display:inline-block;min-width:26px;text-align:center;border-radius:8px;padding:2px 8px;
  font-weight:800;color:#fff;font-size:13px}
.rt-mk.b1{background:#e11d48}.rt-mk.b2{background:#f97316}.rt-mk.b3{background:#f59e0b}
.rt-mk.b4{background:#0891b2}.rt-mk.b5{background:#12a150}
.rt-pin{letter-spacing:.6em;text-align:center;font-size:20px!important;font-weight:800}
.rt-mtab th,.rt-mtab td{padding:7px 8px}
.rt-mtab .rt-mc{text-align:center;white-space:nowrap}
.rt-mtab tr.rt-grow td{background:var(--chip);font-weight:800;font-size:13px;color:var(--ink2)}
.rt-mk0{display:inline-block;min-width:26px;text-align:center;color:var(--muted);font-weight:700}
.rt-mkcell{display:flex;flex-direction:column;align-items:center;gap:3px}
.rt-mksub{font-size:10.5px;color:var(--muted);font-weight:600;white-space:nowrap}
/* --- Darsdagi topshiriq uchun qo'lda baho --- */
.rt-manual{margin-top:3px;border:1.5px dashed var(--line);background:none;color:var(--muted);
  border-radius:8px;padding:2px 8px;font-size:11px;font-weight:800;cursor:pointer;font-family:inherit;
  transition:.14s;white-space:nowrap}
.rt-manual:hover{border-color:var(--green);color:var(--green)}
.rt-manual.on{border-style:solid;border-color:var(--green);
  background:color-mix(in srgb,var(--green) 18%,transparent);color:var(--green)}
.rt-okban{display:flex;align-items:center;gap:11px;border:1.5px solid var(--green);
  background:color-mix(in srgb,var(--green) 10%,transparent);border-radius:14px;
  padding:10px 13px;margin-bottom:12px;flex-wrap:wrap}
.rt-okban .ic{font-size:22px}
.rt-okban .t{flex:1;min-width:150px}
.rt-okban .t b{display:block;font-size:14px}
.rt-okban .t span{display:block;font-size:12px;color:var(--muted)}
.rt-from{background:var(--panel);border:1px solid var(--line);border-radius:12px;
  padding:10px 12px;margin-bottom:10px}
.rt-from label{display:block;font-size:12.5px;font-weight:800;color:var(--muted);margin-bottom:5px}
.rt-from input{width:100%;max-width:340px;border:1px solid var(--line);background:var(--panel2);
  color:var(--ink);border-radius:10px;padding:8px 11px;font-family:inherit;font-size:14px}
.rt-from .rt-note{margin-top:6px}
/* --- Ustozga xabar (topbar tugmalari yonida, biroz kattaroq) --- */
.rt-ask{display:inline-flex;align-items:center;gap:7px;border-radius:999px;
  padding:8px 16px;font-size:14.5px;font-weight:800;text-decoration:none;color:#fff;
  background:linear-gradient(135deg,#229ED9,#1b7fb0);border:1px solid transparent;
  box-shadow:0 8px 18px -10px rgba(34,158,217,.9);transition:.18s;white-space:nowrap}
.rt-ask:hover{transform:translateY(-2px);box-shadow:0 12px 24px -10px rgba(34,158,217,1)}
.rt-ask b{font-weight:800;color:#fff}
@media(max-width:860px){ .rt-ask{padding:7px 13px;font-size:13.5px} }
@media print{.rt-ask{display:none!important}}
/* --- Fanlar ro'yxati --- */
.rt-courses{display:flex;flex-direction:column;gap:10px;margin-top:14px}
.rt-course{display:flex;align-items:center;gap:14px;width:100%;text-align:left;
  border:1.5px solid var(--line);background:var(--panel2);color:var(--ink);
  border-radius:16px;padding:14px 16px;cursor:pointer;font-family:inherit;transition:.18s}
.rt-course:hover{border-color:var(--violet);transform:translateY(-2px);box-shadow:0 10px 22px -14px rgba(0,0,0,.4)}
.rt-course .ic{font-size:30px;line-height:1}
.rt-course .t{flex:1;min-width:0}
.rt-course .t b{display:block;font-size:15.5px}
.rt-course .t span{display:block;font-size:12.5px;color:var(--muted);margin-top:2px;line-height:1.4}
.rt-course .ar{font-size:20px;font-weight:900;color:var(--violet)}
/* --- Ustoz e'loni: talaba tomonida katta modal --- */
.rt-annwrap{z-index:200;background:rgba(8,15,29,.68);backdrop-filter:blur(3px)}
.rt-annbox{max-width:520px;text-align:center;border:2px solid var(--violet)}
.rt-annic{font-size:52px;line-height:1}
.rt-anntxt{background:var(--panel2);border-radius:16px;padding:16px 18px;margin-top:6px;
  font-size:15.5px;line-height:1.7;text-align:left;white-space:pre-wrap;word-break:break-word;
  max-height:50vh;overflow:auto}
/* --- Ustoz e'loni: o'qituvchi paneli --- */
.rt-ann textarea{width:100%;min-height:100px;border:1px solid var(--line);border-radius:12px;padding:10px 12px;
  font-family:inherit;font-size:14px;background:var(--panel);color:var(--ink);resize:vertical}
/* --- Telefonda topbarni yig'ib-yozish --- */
.tb-toggle{display:none}
@media(max-width:860px){
  .topbar{transition:max-height .32s cubic-bezier(.4,0,.2,1),padding .32s ease,opacity .22s ease,
    border-color .32s ease;max-height:600px;overflow:hidden}
  body.tb-collapsed .topbar{max-height:0!important;padding-top:0!important;padding-bottom:0!important;
    opacity:0;pointer-events:none;border-bottom-color:transparent}
  .tb-toggle{display:flex;align-items:center;justify-content:center;position:fixed;top:4px;left:50%;
    transform:translateX(-50%);z-index:55;width:56px;height:28px;border:1.5px solid var(--violet);
    border-top:none;border-radius:0 0 14px 14px;background:var(--panel);color:var(--violet);
    font-size:18px;font-weight:800;line-height:1;cursor:pointer;box-shadow:0 8px 16px -8px rgba(0,0,0,.4);
    font-family:inherit;transition:top .32s ease}
  body.tb-collapsed .tb-toggle{top:0}
}
@media print{.tb-toggle{display:none!important}}
/* --- Ustoz e'lonidagi rasm --- */
.rt-annimg{display:block;width:100%;max-height:46vh;object-fit:contain;border-radius:14px;
  background:var(--panel2);margin-bottom:12px;box-shadow:0 8px 20px -12px rgba(0,0,0,.35)}
.rt-annprev{position:relative;display:inline-block;margin-bottom:10px;max-width:100%}
.rt-annprev img{display:block;max-width:100%;max-height:220px;border-radius:12px;
  border:1px solid var(--line);object-fit:contain;background:var(--panel2)}
.rt-annprev #annImgX{position:absolute;top:-8px;right:-8px;width:26px;height:26px;border-radius:50%;
  border:none;background:var(--red,#e11d48);color:#fff;font-weight:900;font-size:13px;cursor:pointer;
  box-shadow:0 4px 10px -4px rgba(0,0,0,.5);line-height:1}
/* --- So'rovnoma: talaba tomonida --- */
.rt-poll{background:var(--panel2);border-radius:16px;padding:16px 18px;margin-top:12px;text-align:left}
.rt-poll b{display:block;font-size:15px;margin-bottom:10px}
.rt-poll-opt{display:block;width:100%;text-align:left;padding:11px 14px;margin-bottom:8px;
  border:1px solid var(--line);border-radius:12px;background:var(--panel);color:var(--ink);
  font-family:inherit;font-size:14px;cursor:pointer;transition:border-color .15s ease,background .15s ease}
.rt-poll-opt:hover{border-color:var(--violet)}
.rt-poll-opt.sel{border-color:var(--violet);background:rgba(139,92,246,.12);font-weight:700}
/* --- So'rovnoma: o'qituvchi qurish paneli --- */
.rt-pollbuild{margin-top:14px;padding-top:12px;border-top:1px dashed var(--line)}
.rt-poll-row{display:flex;gap:8px;align-items:center;margin-bottom:8px}
.rt-poll-in{flex:1;border:1px solid var(--line);border-radius:10px;padding:9px 11px;
  font-family:inherit;font-size:14px;background:var(--panel);color:var(--ink)}
#pollQ.rt-poll-in{width:100%}
.rt-poll-del{flex:0 0 auto;width:30px;height:30px;border-radius:50%;border:none;
  background:var(--red,#e11d48);color:#fff;font-weight:900;font-size:13px;cursor:pointer;line-height:1}
/* --- So'rovnoma: natijalar --- */
.rt-pollres{margin-top:14px;padding-top:12px;border-top:1px dashed var(--line)}
.rt-pollres .row{display:flex;justify-content:space-between;gap:10px;font-size:13.5px;margin:6px 0 3px}
.rt-pollres .bar{height:8px;border-radius:5px;background:var(--panel2);overflow:hidden;margin-bottom:8px}
.rt-pollres .bar i{display:block;height:100%;background:var(--violet);border-radius:5px}
/* --- Bir nechta e'lon: talaba tomonida ro'yxat --- */
.rt-ann-items{max-height:58vh;overflow:auto;text-align:left;margin-top:8px}
.rt-ann-item{margin-bottom:4px}
.rt-ann-ord{font-weight:800;font-size:12.5px;color:var(--violet);text-transform:uppercase;
  letter-spacing:.3px;margin-bottom:6px}
.rt-ann-sep{border:none;border-top:1px dashed var(--line);margin:18px 0}
/* --- Bir nechta e'lon: o'qituvchi paneli ro'yxati --- */
.rt-annlist{padding-top:4px}
.rt-annitem{background:var(--panel);border:1px solid var(--line);border-radius:14px;
  padding:12px 14px;margin-bottom:10px}
.rt-annitem-head{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-bottom:8px}
.rt-annitem-head b{font-size:13.5px}
.rt-annitem-head .rt-note{flex:1}
.rt-annimg-sm{display:block;max-width:100%;max-height:140px;border-radius:10px;
  object-fit:contain;background:var(--panel2);margin-bottom:8px}
.rt-anntxt-sm{font-size:13.5px;line-height:1.6;white-space:pre-wrap;word-break:break-word}
.rt-pollq{margin-top:8px}
.rt-pollq b{font-size:13.5px}
