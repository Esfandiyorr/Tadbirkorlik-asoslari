/* ================== MUSIQA PLEYERI ================== */
#mzTab{position:fixed;right:0;top:50%;transform:translateY(-50%);z-index:70;
  background:linear-gradient(135deg,var(--violet),var(--pink));color:#fff;border:none;
  border-radius:14px 0 0 14px;padding:12px 9px;cursor:pointer;box-shadow:var(--shadow);
  font-size:19px;line-height:1;transition:.2s}
#mzTab:hover{padding-right:13px}
#mzTab.on{background:linear-gradient(135deg,var(--green),var(--teal))}
#mzPanel{position:fixed;right:-340px;top:50%;transform:translateY(-50%);z-index:71;width:320px;max-width:92vw;
  background:var(--panel);border:1px solid var(--line);border-radius:18px 0 0 18px;border-right:none;
  box-shadow:var(--shadow);padding:16px;transition:right .32s cubic-bezier(.22,1,.36,1)}
#mzPanel.show{right:0}
#mzPanel h3{margin:0 0 10px;font-size:16px;display:flex;align-items:center;gap:8px}
#mzPanel h3 span{flex:1}
#mzPanel .x{border:none;background:transparent;color:var(--muted);font-size:17px;cursor:pointer;line-height:1}
.mz-list{max-height:38vh;overflow:auto;margin:0 0 12px;padding:0;list-style:none}
.mz-list li{display:flex;align-items:center;gap:9px;padding:8px 10px;border-radius:11px;cursor:pointer;
  font-size:13.5px;font-weight:600;color:var(--ink2);transition:.15s}
.mz-list li:hover{background:var(--chip)}
.mz-list li.on{background:color-mix(in srgb,var(--violet) 16%,transparent);color:var(--violet)}
.mz-list li i{font-style:normal;font-size:15px;width:18px;text-align:center}
.mz-list li b{flex:1;font-weight:700}
.mz-list li span{font-size:11.5px;color:var(--muted);font-weight:600}
.mz-now{background:var(--panel2);border-radius:13px;padding:11px 12px;margin-bottom:10px}
.mz-now b{display:block;font-size:14px}
.mz-now span{font-size:12px;color:var(--muted)}
.mz-bar{height:6px;border-radius:99px;background:var(--chip);margin:9px 0 4px;cursor:pointer;position:relative}
.mz-bar i{display:block;height:100%;border-radius:99px;background:linear-gradient(90deg,var(--violet),var(--pink));width:0}
.mz-time{display:flex;justify-content:space-between;font-size:11px;color:var(--muted);font-variant-numeric:tabular-nums}
.mz-ctrl{display:flex;align-items:center;gap:8px;justify-content:center;margin-top:8px}
.mz-btn{border:1px solid var(--line);background:var(--panel2);color:var(--ink);border-radius:11px;
  width:38px;height:38px;display:grid;place-items:center;cursor:pointer;font-size:15px;transition:.15s}
.mz-btn:hover{border-color:var(--violet);transform:translateY(-2px)}
.mz-btn.play{width:46px;height:46px;font-size:18px;background:linear-gradient(135deg,var(--violet),var(--blue));
  color:#fff;border-color:transparent}
.mz-btn.on{border-color:var(--green);color:var(--green)}
.mz-vol{display:flex;align-items:center;gap:8px;margin-top:10px}
.mz-vol input{flex:1;accent-color:var(--violet)}
.mz-note{font-size:11.5px;color:var(--muted);margin:8px 0 0;line-height:1.45}
@media(max-width:640px){
  #mzPanel{width:88vw;top:auto;bottom:60px;transform:none;border-radius:18px 0 0 18px}
  /* chat bilan bir ustunda, uning tepasida */
  #mzTab{top:calc(var(--tbh,104px) + 18px);bottom:auto;transform:none;
    width:42px;height:42px;padding:0;display:flex;align-items:center;justify-content:center;
    border-radius:12px 0 0 12px;font-size:19px;opacity:.8}
  #mzTab:hover{padding-right:0}
  #mzTab:active{opacity:1}
}
@media print{#mzTab,#mzPanel{display:none!important}}
